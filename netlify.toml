[build]
  command = "npm install && npm run build"
  publish = "dist"
  base = "frontend"

# Environment variables are set in Netlify dashboard
# VITE_API_BASE_URL should be set to: https://reportifybackend.vercel.app

[[redirects]]
  from = "/api/*"
  to = "https://reportifybackend.vercel.app/api/:splat"  
  status = 200
  force = true

# Handle client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200