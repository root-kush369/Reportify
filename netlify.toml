[build]
  command = "npm install && npm run build"
  publish = "dist"
  base = "frontend"

[[redirects]]
  from = "/api/*"
  to = "https://reportifybeun.vercel.app//api/:splat"  // Use the new URL
  status = 200
  force = true